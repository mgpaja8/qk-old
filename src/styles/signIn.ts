import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  containerView: {
    paddingLeft: 5,
    paddingRight: 5,
    height: '100%',
    alignItems: 'center',
    justifyContent: 'center'
  },
  rowContainerView: {
    flexDirection: 'row',
    justifyContent: 'center'
  }
});
